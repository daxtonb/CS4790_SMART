@page
@model Smart.Pages.Application.IndexModel

@{
    ViewData["Title"] = "Index";
}

@*
    TO DO
        - Only show applicants! (non students)
        - Add search table search functionality
        - show scores
        - Rated checkbox
        - FIX UserId & TermId for ApplicantRating!!!!
        - Add new fields to student.
        - Rename Criterium variables and everything & add migration (Tell everyone to update)
*@ 

<h2>Applicants</h2><br/>

<table class="table table-striped border col-md-12" id="applicantTable">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Student[0].FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student[0].LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student[0].PublicSchoolLevel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student[0].StudentStatus)
            </th>
            <th>
                Rated?
            </th>
            <th>
                Composite Score
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Student)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PublicSchoolLevel)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StudentStatus.Description)
            </td>
            <td>
                <input type="checkbox" name="isRated" value="idk" onclick="return false">  @*needs to be tied to something, the user can't check them*@
            </td>
            <td>
                
            </td>

            @*Button Group*@
            <td style="width:150px">
                <div class="btn-group" role="group">
                    <a class="btn btn-warning text-white" asp-page="Rating" asp-route-Id="@item.StudentId">
                        <i class="fas fa-star"></i>
                    </a>
                    <a class="btn btn-primary text-white" asp-page="Edit" asp-route-id="@item.StudentId">
                        <i class="fas fa-edit"></i>
                    </a>
                    <a class="btn btn-success text-white" asp-page="Details" asp-route-Id="@item.StudentId">
                        <i class="fas fa-list-alt"></i>
                    </a>
                    <a class="btn btn-danger text-white" asp-page="Delete" asp-route-Id="@item.StudentId">
                        <i class="fas fa-trash-alt"></i>
                    </a>
                </div>
            </td>

            @*<td>
            <a asp-page="./Edit" asp-route-id="@item.StudentId">Edit</a> |
            <a asp-page="./Details" asp-route-id="@item.StudentId">Details</a> |
            <a asp-page="./Delete" asp-route-id="@item.StudentId">Delete</a>
        </td>*@
        </tr>
        }
    </tbody>
</table>

<a asp-page="Create" class="btn btn-info"><i class="fas fa-plus"></i> &nbsp;  Create New Applicant </a>